#include <iostream>
#include "vector.h"

int main(void) {

	//vector<int> a(10, 0);
	//for (int i = 0; i < a.size(); i++) {
	//	a[i] = i;
	//	std::cout << a[i] << " ";
	//}
	//a.clear();

	return 0;
}
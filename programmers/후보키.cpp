#include <string>
#include <vector>
#include <set>

using namespace std;

int solution(vector<vector<string>> relation) {

    vector<set<string>> a;
    set<string> s;
    for (const auto& v : relation)
    {
        ;
    }
    return ;
}

int main()
{
    solution();
    return 0;
}